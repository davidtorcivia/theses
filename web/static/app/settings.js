// The per proposition settings page. The form is rendered by the server; this
// is the rail beside it, the initials of whoever else is here, and the palette.

import { start } from './chrome.js';

start(() => {});
